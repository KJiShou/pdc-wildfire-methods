{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Parallel QOI native result",
  "type": "object",
  "required": ["status", "backend", "input", "configuration", "timing", "output", "chunks", "cross_block", "validation"],
  "properties": {
    "status": {"enum": ["success", "validation_failed", "error"]},
    "backend": {"enum": ["serial", "one-pass", "openmp", "cuda", "mpi"]},
    "error": {"type": "string"},
    "input": {
      "type": "object",
      "required": ["path", "width", "height", "channels"],
      "properties": {
        "path": {"type": "string"},
        "width": {"type": "integer", "minimum": 0},
        "height": {"type": "integer", "minimum": 0},
        "channels": {"type": "integer", "enum": [0, 3, 4]}
      }
    },
    "configuration": {
      "type": "object",
      "properties": {
        "persistent_context_reused": {"type": "boolean"},
        "input_cache_reused": {"type": "boolean"}
      }
    },
    "timing": {
      "type": "object",
      "required": ["load_ms", "cuda_init_ms", "openmp_init_ms", "allocation_ms", "summary_ms", "propagation_ms", "transfer_in_ms", "encode_ms", "transfer_out_ms", "merge_ms", "prefix_scan_ms", "compaction_ms", "core_pipeline_ms", "write_ms", "metrics_analysis_ms", "validation_ms", "total_ms"],
      "additionalProperties": {"type": "number", "minimum": 0}
    },
    "output": {
      "type": "object",
      "required": ["path", "bytes", "bmp_bytes", "compression_ratio", "throughput_mpixels", "core_pipeline_throughput_mpixels"],
      "properties": {
        "path": {"type": "string"},
        "bytes": {"type": "integer", "minimum": 0},
        "bmp_bytes": {"type": "integer", "minimum": 0},
        "compression_ratio": {"type": "number", "minimum": 0},
        "throughput_mpixels": {"type": "number", "minimum": 0},
        "core_pipeline_throughput_mpixels": {"type": "number", "minimum": 0}
      }
    },
    "chunks": {
      "type": "object",
      "required": ["run", "index", "diff", "luma", "rgb", "rgba"],
      "additionalProperties": {"type": "integer", "minimum": 0}
    },
    "cross_block": {
      "type": "object",
      "required": ["inherited_index_hits", "fallback_bytes_avoided"],
      "additionalProperties": {"type": "integer", "minimum": 0}
    },
    "validation": {
      "type": "object",
      "required": ["passed", "decoder_accepted", "dimensions_match", "channels_match", "pixel_match"],
      "additionalProperties": {"type": "boolean"}
    },
    "experiment": {
      "type": "object",
      "properties": {
        "process_wall_ms": {"type": "number", "minimum": 0},
        "request_roundtrip_ms": {"type": "number", "minimum": 0},
        "worker_startup_ms": {"type": "number", "minimum": 0},
        "worker_reused": {"type": "boolean"},
        "input_cache_reused": {"type": "boolean"},
        "persistent_mpi": {"type": "boolean"}
      }
    }
  }
}
